{
  "id": "bezier",
  "label": "Bezier Curve",
  "category": "Geometry",
  "description": "Draw bezier curves interactively. Click to place corner points, click-and-drag to pull out curve handles. Snaps to existing geometry.",
  "inputs": [],
  "outputs": [
    { "id": "geometry_out", "label": "Geometry Out", "type": "geometry" }
  ],
  "parameters": [
    { "id": "anchors_data", "label": "Anchors", "type": "text", "default": "[]" },
    { "id": "closed", "label": "Close Path", "type": "boolean", "default": false },
    { "id": "stroke_color", "label": "Stroke Color", "type": "color", "default": "#000000" },
    { "id": "stroke_width", "label": "Stroke Width", "type": "number", "default": 2, "min": 0.5, "max": 20 }
  ],
  "runtime": "bezier"
}
